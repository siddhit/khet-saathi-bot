[project]
name = "agripulse-bot"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["requests==2.32.3", "anthropic"]

[tool.vercel]
entrypoint = "api.webhook:handler"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
